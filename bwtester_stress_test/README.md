# test bandwidth tester and SIG

**makeLogFolders.py** --  creates folder structure for logs
**run_bwtest.py** --  uses docker to attach to container and run bandwidth test
**scion-seed.py** --  creates docker topology

